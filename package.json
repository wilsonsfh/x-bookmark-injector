{
  "name": "x-bookmark-injector",
  "version": "0.1.0",
  "description": "A personal Chromium extension that resurfaces random X bookmarks in the For You feed.",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wilsonsfh/x-bookmark-injector.git"
  },
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.2.6"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
